# XR Webflow Scripts

JavaScript and CSS files hosted for use across XR Webflow sites.

## Usage

Files in this repo are intended to be referenced directly in Webflow project settings or embedded via `<script>` and `<link>` tags.

## Structure

- `js/` — JavaScript files
- `css/` — CSS files
